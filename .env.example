# PixelLab AI — get a key at https://www.pixellab.ai (account → API).
# Set it as an environment secret in the Claude Code web environment (preferred),
# or copy this file to .env (gitignored) for local use. NEVER commit the real key.
PIXELLAB_API_KEY=your-key-here
